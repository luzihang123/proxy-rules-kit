# 订阅管理

公开仓库只保留 `subscriptions/subscriptions.example.yaml`。真实文件应复制为 `subscriptions/subscriptions.yaml`，并保持本地私有。

不要公开：

- 订阅 URL 和 token。
- 服务商后台地址。
- 家庭成员或账号归属。
- 内网地址、NAS 地址、穿透域名。

如果需要合并订阅，可以把 subconverter、Sub-Store、proxy-providers 等配置放在私有仓库或本地忽略文件里。
