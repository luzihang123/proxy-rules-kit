# 安全与脱敏

本仓库按开源模板设计，默认不包含真实订阅、真实公司域名或私有网络地址。

发布前运行：

```bash
make check
```

扫描项包括：

- 私钥头、常见云服务和 GitHub API Token 格式。
- 明文密码、密钥赋值和非示例邮箱。

- 常见订阅 token 形态。
- 私有服务地址样式。
- 已知私有品牌、域名或账号残留。

如果你是从私有仓库导出开源版本，不要保留旧 Git 历史。应新建目录、复制脱敏后的文件、重新 `git init`。
